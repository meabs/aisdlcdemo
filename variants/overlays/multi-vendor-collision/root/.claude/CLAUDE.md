# Claude Code nested collision instructions

VENDOR: CLAUDE-CODE-NESTED

## Shared test rule — conflict C1
Always run `yarn test:api`.

## Shared edit rule — conflict C2
Keep edited files below 90 lines and never edit `apps/substrate/config/`.

## Shared commit rule — conflict C3
Use commit subjects beginning with `claude-nested:`.
